;; Standard template for the assembly text format.

(code
    (ip-set "entry_point")
    
    (label "entry_point")
    (record.alloc 0)        ;; ()
    (call "main")           ;; () main
    (closure.apply)
    (exit 0)

    ;; main
    (proc "main" (args 0))
        (closure.alloc (proc "main[closure`0]") (args 0))
    (return)

        (proc "main[closure`0]" (args 1)) ;; ->
            ;; code goes here
        (return)
    ;; end main
)